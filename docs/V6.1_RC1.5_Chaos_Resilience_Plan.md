/**
 * RC-1.5 — Chaos & Resilience Testing Plan (Medical Copilot v1.0)
 *
 * Purpose: validate safe, governed, predictable behavior under adverse conditions.
 * Scope: frontend resilience against existing architecture (no clinical logic changes).
 *
 * Automated coverage: lib/medical-copilot/resilience.chaos.test.ts
 * Manual / staging items: documented in Results as Partial / Fail where not automatable.
 */

export const RC15_CHAOS_PLAN = {
  phase: "RC-1.5",
  version: "v1.0",
  scenarios: [
    "facade_timeout",
    "transient_network_loss",
    "speech_provider_error_fallback",
    "auth_expiration_safety",
    "session_ownership_single_id",
    "kill_switch_isolation",
    "telemetry_phi_safe_under_failure",
    "workflow_recovery_preserve_session",
  ],
  constraints: {
    noNewProductModules: true,
    noCoreChanges: true,
    noGovernanceChanges: true,
    noPublicApiChanges: true,
  },
} as const;
