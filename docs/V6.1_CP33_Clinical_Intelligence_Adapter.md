/**
 * CP-33 — Clinical Intelligence Adapter Foundation
 *
 * Pipeline:
 * Clinical Dictation → Clinical Intelligence Adapter → Medical Copilot Facade API
 * → Backend AI Governance → Clinical Intelligence → typed response → UI
 *
 * Scope: foundation only (types, mapper, adapter, hooks, tests).
 * No local LLM, prompts, streaming, WebSockets, EMR writes, or UI wiring.
 */

export const CP33_CLINICAL_INTELLIGENCE_ADAPTER = {
  phase: "CP-33",
  module: "lib/medical-copilot/clinical-intelligence",
  consumes: "/medical-copilot/* Facade API only",
  hitl: true,
  storesClinicalResults: false,
  localLlm: false,
} as const;
